spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/crm
    username: postgres
    password: java

  jpa:
    database: POSTGRESQL
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

  sql:
    init:
      mode: always